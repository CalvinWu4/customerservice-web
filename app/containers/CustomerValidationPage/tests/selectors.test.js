// import { fromJS } from 'immutable';
// import { selectCustomerValidationPageDomain } from '../selectors';

describe('selectCustomerValidationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
