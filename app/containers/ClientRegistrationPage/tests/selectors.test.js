// import { fromJS } from 'immutable';
// import { selectClientRegistrationPageDomain } from '../selectors';

describe('selectClientRegistrationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
